import React from 'react';
import './App.css';
import {useRoutes}  from 'hookrouter';

const App: React.FC = () => {
  const routeResult = useRou
  return (
    <VerticalBarChart />
  );
}

export default App;
